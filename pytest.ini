[pytest]
testpaths = tests
# Explicit so the repo root is importable however pytest is invoked — VS Code's
# test explorer does not necessarily run from the project directory.
pythonpath = .
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
